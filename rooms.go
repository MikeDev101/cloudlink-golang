package main

// broken, dont use for now
/* package main

var rooms = []string{}

func CreateRoom(room string) {
	if !containsValue(rooms, room) {
		rooms = append(rooms, room)
	}
}
func RemoveRoom(room string) {
	if GetValue(rooms, room) != -1 {
		rooms = RemoveValue(rooms, GetValue(rooms, room))
	}
}
func RoomExists(room string) bool {
	if GetValue(rooms, room) != -1 {
		return true
	} else {
		return false
	}
}
*/
